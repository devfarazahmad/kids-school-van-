import 'package:flutter/material.dart';

class DriverStatusView extends StatelessWidget {
  const DriverStatusView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("One-Tap Status Broadcast")),
    );
  }
}
