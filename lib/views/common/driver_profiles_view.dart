import 'package:flutter/material.dart';

class DriverProfilesView extends StatelessWidget {
  const DriverProfilesView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("View Student Profiles")),
    );
  }
}
