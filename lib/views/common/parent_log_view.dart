import 'package:flutter/material.dart';

class ParentLogView extends StatelessWidget {
  const ParentLogView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("Digital Travel Log")),
    );
  }
}
