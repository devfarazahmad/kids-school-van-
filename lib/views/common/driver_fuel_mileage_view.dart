import 'package:flutter/material.dart';

class DriverFuelMileageView extends StatelessWidget {
  const DriverFuelMileageView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("Fuel & Mileage Log")),
    );
  }
}
